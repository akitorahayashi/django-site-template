
#
# このファイルをテンプレートとして、各環境用の.env.*ファイルを作成してください。
# (例: .env.dev, .env.prod, .env.test)
#
# Docker Composeはデフォルトで`.env`ファイルを読み込みます。
# Makefileの各コマンドは、実行環境に応じて適切な.env.*ファイルへの
# シンボリックリンクを`.env`として自動的に作成します。

# Environment identifier for volume naming
ENV=dev

# http://127.0.0.1:50080/
HOST_BIND_IP=127.0.0.1
HOST_PORT=50080

# ---------------------------------------------------------------------------
# POSTGRESQL DATABASE SETTINGS
# ---------------------------------------------------------------------------
# These variables are used by the 'db' service in docker-compose.yml
# and to construct the DATABASE_URL below.
# For .env.test, it is recommended to change DB_NAME to 'django_db_test'.
DB_NAME=django_db
DB_USER=django_user
DB_PASSWORD=django_password

# PostgreSQL Database Connection URL for Django
# This URL is constructed from the variables above.
# You should not need to change this line directly.
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@db:5432/${DB_NAME}# .env.example