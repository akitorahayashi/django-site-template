
# This file is a template for the base .env file.
# Copy this file to .env and fill in the values for your production environment.
#
# The application now uses a layered environment variable system:
#   - .env: Base configuration (production defaults)
#   - .env.development: Overrides for the development environment (DJANGO_ENV=development)
#   - .env.test: Overrides for the test environment (DJANGO_ENV=test)
#   - .env.local: Local overrides (not in Git) for personal settings.
#
# The loading priority is: .env.local > .env.{DJANGO_ENV} > .env

# ---------------------------------------------------------------------------
# DJANGO SETTINGS
# ---------------------------------------------------------------------------
# Set to 'False' in production for security.
# It is automatically set to 'True' in the development environment via .env.development.
DEBUG=False

# A secure, unique secret key is required for production.
# Generate one with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-secret-key-goes-here

# ---------------------------------------------------------------------------
# DATABASE SETTINGS
# ---------------------------------------------------------------------------
# PostgreSQL connection URL for Django.
# Format: postgresql://USER:PASSWORD@HOST:PORT/NAME
DATABASE_URL=postgresql://django_user:django_password@db:5432/django_db

# ---------------------------------------------------------------------------
# DOCKER-COMPOSE SETTINGS
# ---------------------------------------------------------------------------
# These variables are used by docker-compose.yml to configure services.
# They are not directly used by Django, but are related to the database setup.
DB_NAME=django_db
DB_USER=django_user
DB_PASSWORD=django_password
HOST_BIND_IP=127.0.0.1
HOST_PORT=50000